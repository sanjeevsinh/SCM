@model SCM.Models.ViewModels.BundleInterfaceVlanViewModel

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

@if (ViewData["ConcurrencyErrorMessage"] != null) {
<div class="alert alert-danger">@ViewData["ConcurrencyErrorMessage"]</div>
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Vlan</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.VlanTag)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.VlanTag)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="BundleInterfaceVlanID" />
        <input type="hidden" asp-for="BundleInterfaceID" />        
        <input type="hidden" asp-for="RowVersion" />
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="GetAllByBundleInterfaceID" asp-route-id="@Model.BundleInterfaceID">Back to List</a>
        </div>
    </form>
</div>
