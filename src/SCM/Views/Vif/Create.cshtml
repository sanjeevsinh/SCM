@model SCM.Models.ViewModels.VifRequestViewModel

@{
    ViewData["Title"] = "Create";
}

<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/Home">Home</a></li>
    <li class="breadcrumb-item"><a href="/Tenant/GetAll">Tenants</a></li>
    <li class="breadcrumb-item"><a href="/Attachment/GetAllByTenantID/@ViewBag.Attachment.TenantID">Attachments</a></li>
    <li class="breadcrumb-item"><a href="/Vif/GetAllByAttachmentID/@ViewBag.Attachment.ID">VIFs</a></li>
    <li class="breadcrumb-item active">Create</li>
</ol>

<h2>Create VIF for Attachment '@ViewBag.Attachment.Name'</h2>

<form asp-action="Create">
    <div class="form-horizontal">
        <hr />
        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
        <input type="hidden" asp-for="AttachmentID" value="@ViewBag.Attachment.ID" />
        <div class="form-group">
            <label asp-for="Tenant" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <select asp-for="TenantID" class="form-control" asp-items="ViewBag.TenantID">
                    <option value="">-- Select Tenant --</option>
                </select>
                <span asp-validation-for="TenantID" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="ContractBandwidthPool" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <select asp-for="ContractBandwidthPoolID" class="form-control" asp-items="ViewBag.ContractBandwidthPoolID">
                    <option value="">Select Contract Bandwidth Pool</option>
                </select>
                <span asp-validation-for="ContractBandwidthPoolID" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="AutoAllocateVlanTag" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="AutoAllocateVlanTag" class="form-control"/>
                <span asp-validation-for="AutoAllocateVlanTag" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="RequestedVlanTag" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="RequestedVlanTag" class="form-control" />
                <span asp-validation-for="RequestedVlanTag" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="IsLayer3" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="IsLayer3" class="form-control" />
                <span asp-validation-for="IsLayer3" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="IpAddress" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="IpAddress" class="form-control" />
                <span asp-validation-for="IpAddress" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="SubnetMask" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="SubnetMask" class="form-control" />
                <span asp-validation-for="SubnetMask" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="GetAllByAttachmentID" asp-route-id="@ViewBag.Attachment.ID">Back to VIFs List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
   
    <script type="text/javascript">

        (function ($) {

            var $isLayer3 = $('#IsLayer3'),
            isLayer3 = $isLayer3[0],
            $ipAddress = $('#IpAddress'),
            ipAddress = $ipAddress[0],
            $subnetMask = $('#SubnetMask'),
            subnetMask = $subnetMask[0],
            $contractBandwidthPool = $('#ContractBandwidthPoolID'),
            contractBandwidthPool = $contractBandwidthPool[0],
            $tenant = $('#TenantID'),
            tenant = $tenant[0],
            $autoAllocateVlanTag = $('#AutoAllocateVlanTag'),
            autoAllocateVlanTag = $autoAllocateVlanTag[0];
            $requestedVlanTag = $('#RequestedVlanTag'),
            requestedVlanTag = $requestedVlanTag[0];

            if (autoAllocateVlanTag.checked) {

                requestedVlanTag.value = null;
                requestedVlanTag.disabled = true;
            }
            else {

                requestedVlanTag.disabled = false;
            }

            $autoAllocateVlanTag.on('click', function (e) {

                if (this.checked) {

                    requestedVlanTag.value = null;
                    requestedVlanTag.disabled = true;
                }
                else {

                    requestedVlanTag.disabled = false;
                }
            });

            if (!isLayer3.checked) {

                ipAddress.disabled = true;
                subnetMask.disabled = true;
            }

            $isLayer3.on('click', function (e) {

                var isLayer3Checked = this.checked;
                ipAddress.disabled = !isLayer3Checked;
                subnetMask.disabled = !isLayer3Checked;

                if (isLayer3Checked) {

                    contractBandwidthPool.disabled = false;
                    isTagged.checked = false;
                }
                else
                {
                    ipAddress.value = null;
                    subnetMask.value = null;
                }
            });

            $tenant.on('change', function (e) {

                var tenantID = this.value;

                $.get("/Vif/ContractBandwidthPools", { id: tenantID })
                .done(function (data) {
                    $contractBandwidthPool.html(data);
                });
            });

        }(jQuery));

    </script>
}